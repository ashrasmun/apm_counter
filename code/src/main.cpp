#include <windows.h>

int CALLBACK
WinMain(HINSTANCE hInstance,
        HINSTANCE hPrevInstance,
        LPSTR lpCmdLine,
        int nCmdShow)
{
    /* TODO: Fix the TODO snippet <01-05-20, ashrasmun> */
    /* TODO: change the todo snippet, so that the nick in on the right of comma <01-05-20, ashrasmun> */
    /* TODO: add syntax constraints same to python ones in vimrc <01-05-20, ashrasmun> */
    UNREFERENCED_PARAMETER(hInstance);
    UNREFERENCED_PARAMETER(hPrevInstance);
    UNREFERENCED_PARAMETER(lpCmdLine);
    UNREFERENCED_PARAMETER(nCmdShow);

    MessageBox(0, "Hello :)", "I work!", MB_OK);
    return 0;
}
